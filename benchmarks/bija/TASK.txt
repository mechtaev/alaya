Implement the language specified in SPEC.md, exactly as specified. SPEC.md is the authority on every question of behaviour, including the exact text and position of every diagnostic. The programs in tests/ are a small sample, not the requirement: they exercise a fraction of the specification, and the work is judged by a much larger suite of the same shape that you cannot see. Passing the sample is necessary and far from sufficient, so implement every section of the specification, including the parts no sample program touches. Only files under src/bija/ may be changed.
